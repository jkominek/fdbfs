{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build (Meson)",
            "type": "shell",
            "command": "meson compile -C build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc"
        }
    ]
}